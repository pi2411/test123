
<%- include("partials/header"); -%>

    <h1>Home</h1>
    <p> <%= startingContent %> </p>
  <%  posts.forEach(function(post){ %>
    <section class="head">
    <h1><%=post.title%></h1>
    <p>
    <%=post.content.substring(0, 100) + " ..."%>
    <a href="/posts/<%=post.title%> "data-bf=<%=post.title%>"
   data-af="<%=post.content.substring(0, 100) + " ..."%>>Read More"</a>
    </p>
	</section>
    <% }) %>


<%- include("partials/footer"); -%>
